/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: { unoptimized: true }, // opcional si usas imágenes sin dominio remoto
};

export default nextConfig;
